name: CD

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy (example)
        run: |
          echo "Docelowo na self-hosted runnerze:"
          echo "docker compose pull"
          echo "docker compose up -d"
          echo "restart aplikacji / nginx w razie potrzeby"
